package-as: NaowhSR_zhCN
enable-nolib-creation: no

# 打包时排除开发资料：发布出去的 zip 只含 toc + Dict + Locale
ignore:
  - .gitignore
  - .gitattributes
  - .workbuddy
  - AGENTS.md
  - README.md
  - build
  - docs
  - tools
